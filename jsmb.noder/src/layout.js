(function (window) {

    var Noder = [[['constructor']]];

    Noder.prototype = [[['prototype']]];

    Noder.prototype.constructor = window.Noder;

    [[['static']]];

    window.Noder = Noder;

})(window);